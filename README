To run:

curl -Ls https://raw.githubusercontent.com/lffg/ts/master/install.sh | bash

---

After that, you may install the dependencies (`typescript`, `tslint`, `tslint-config-prettier`), running:

yarn add typescript tslint tslint-config-prettier --dev
